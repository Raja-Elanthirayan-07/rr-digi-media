# Server
PORT=3000
NODE_ENV=development
SESSION_SECRET=

# Admin / notifications
ADMIN_EMAIL=flickout99@gmail.com
BUSINESS_EMAIL=flickout99@gmail.com

# SQLite (recommended for deployments with persistent disk)
# Example (Render persistent disk): DB_PATH=/data/data.db
DB_PATH=

# Uploads directory (recommended for deployments with persistent disk)
# Example (Render persistent disk): UPLOAD_DIR=/data/uploads
UPLOAD_DIR=

# SMTP (email OTP + order notifications)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
# Optional
SMTP_FROM=

# Razorpay (payments)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
